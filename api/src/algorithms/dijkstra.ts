import { PathfinderProps } from "../utils/pathfinder-utils";


export const dijkstra = ({
  startingPosition,
  destination,
  map,
}: PathfinderProps) => {
  console.log("destination: ", destination);
  console.log("startingPosition: ", startingPosition);
};
